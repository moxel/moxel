# Simple Release Manager with Google Analytics

## 

## Set Up

First, make sure a Google Cloud Storage bucket dedicated to host static release files are created: `dummy-release`.

In `server.py`, change 

```
BUCKET = "dummy-release"
```

