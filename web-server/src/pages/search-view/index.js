import React from 'react';
import {Route} from "react-router-dom";
const SearchViews = [
    <Route exact path="/list" key="/list" component={() => (<div>model list view</div>)}/>
];
export default SearchViews;

